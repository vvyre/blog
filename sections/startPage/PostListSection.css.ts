import { style } from '@vanilla-extract/css'

export const GROUP_FRAME = style({
  marginTop: '0.8rem',
  marginBottom: '0.8rem',
})

export const YEAR = style({
  width: '100%',
  textAlign: 'left',
  fontWeight: 600,
  fontFamily: '"IBM Plex Mono", "Pretendard", monospace',
  marginBottom: '0.4rem',
})
